func printArray(arr: integer[], size: integer) -> void 
{
  let n: integer;
  let i: integer; 
  n = size;
  i = 0; 
  while (i<n) { 
    write(arr[i]);
      i = i+1;
  };
  f1 = f1.build(2, 3.5);
  f2 = f2.build(-2.0, 1.0, 0.0);
  new_function.a.b = A;
  read(arr[1][n])
} 